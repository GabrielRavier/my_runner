/*
** EPITECH PROJECT, 2020
** myrunner
** File description:
** Declares game_sounds
*/

#include "sound_with_buffer.h"

struct game_sounds {
    struct sound_with_buffer jump1;
    struct sound_with_buffer jump2;
    struct sound_with_buffer jump3;
    struct sound_with_buffer foot1;
    struct sound_with_buffer foot2;
    struct sound_with_buffer foot3;
    struct sound_with_buffer foot4;
    struct sound_with_buffer wall;
};
